

** Please be patient while the chart is being deployed **
-------------------------------------------------------------------------------

{{- if .Values.deployedBy }}
deployed-by: {{ .Values.deployedBy }}
{{- end }}

{{- if .Values.ingress.enabled }}
Application {{ template "chart.name" . }} available via URL for:
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
  https://{{ $host.host }}{{ . }}
  {{- end }}
{{- end }}
{{- end }}


Access to {{ template "chart.name" . }}
-------------------------------------------------------------------------------

{{- if contains "ClusterIP" .Values.service.type }}
  *) Forward port from POD:
     $ export POD=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app={{ .Chart.Name }}" -o jsonpath="{.items[0].metadata.name}")
     $ kubectl port-forward --namespace {{ .Release.Namespace }} $POD <your_free_local_port>:{{ .Values.ports.containerPort }}
  *)  echo "Visit http://127.0.0.1:<your_free_local_port> to access your application."
{{- else if contains "NodePort" .Values.service.type }}
  *) Check Node IP and Port:
     $ export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[3].status.addresses[0].address}")
     $ export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ template "chart.fullname" . }})
{{- end }}
-------------------------------------------------------------------------------
