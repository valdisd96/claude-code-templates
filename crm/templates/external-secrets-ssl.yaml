{{- if .Values.externalSecrets.enabled }}
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ include "chart.fullname" . }}-ssl
  labels:
    {{- include "chart.labelsConfigmapSecret" . | nindent 4 }}
  annotations:
    helm.sh/hook: pre-install,pre-upgrade
    helm.sh/hook-weight: "-30"
spec:
  {{- with .Values.externalSecrets.secretStoreRef }}
  secretStoreRef:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  refreshInterval: "0"
  target:
    creationPolicy: Owner
    deletionPolicy: Retain
    name: {{ include "chart.fullname" . }}-ssl
  dataFrom:
  - extract:
      decodingStrategy: Auto
      key: {{ include "chart.tplvalues.render" ( dict "value" .Values.externalSecrets.secretPath "context" $ ) }}/{{ include "chart.fullname" . }}/certificates
      version: {{ .Values.appConfig.appSslSecretsVersion | quote }}
{{- end }}
